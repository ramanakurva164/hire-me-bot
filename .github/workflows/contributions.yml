name: Contribution Art

on:
  schedule:
    - cron: "0 0 * * *"  # runs daily (UTC)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  draw:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Git
        run: |
          git config user.name "ramanakurva164"
          git config user.email "ramana1642@gmail.com"

      - name: Run script
        run: node script.js

      - name: Push changes
        run: |
          git push origin HEAD:main
